<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace：用来区别不同的类的名字 -->
<mapper namespace="com.peirato.demo.mapper.UserMapper">

    <!-- 通过Id查询一个用户   -->
    <select id="findUserById" parameterType="Integer" resultType="com.peirato.demo.domain.User">
        select * from user where id = #{id}
    </select>

<!--    &lt;!&ndash; 根据用户名模糊查询用户列表 &ndash;&gt;-->
<!--    &lt;!&ndash;    select * from user where username like '%${value}%'&ndash;&gt;-->
<!--    &lt;!&ndash; select * from user where username like "%"#{value}"%"&ndash;&gt;-->
<!--    <select id="findUserByUsername" parameterType="String" resultType="com.ben.domain.User">-->
<!--        select * from user where username like #{username}-->
<!--    </select>-->
<!--    &lt;!&ndash;  添加用户  &ndash;&gt;-->
<!--    <insert id="insertUser" parameterType="com.ben.domain.User">-->
<!--        <selectKey keyProperty="id" resultType="Integer" order="AFTER">-->
<!--            select LAST_INSERT_ID()-->
<!--        </selectKey>-->
<!--        insert into user (username,birthday,address,sex) values(#{username},#{birthday},#{address},#{sex})-->
<!--    </insert>-->
<!--    &lt;!&ndash; 更新用户 &ndash;&gt;-->
<!--    <update id="updateUserById" parameterType="com.ben.domain.User">-->
<!--        update user-->
<!--        set username = #{username},sex = #{sex},birthday = #{birthday},address = #{address}-->
<!--        where id = #{id}-->
<!--    </update>-->
<!--    &lt;!&ndash; 删除用户   &ndash;&gt;-->
<!--    <delete id="deleteUserById" parameterType="Integer">-->
<!--        delete from user-->
<!--        where id = #{vvvvv}-->
<!--    </delete>-->
</mapper>